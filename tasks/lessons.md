- [2026-07-16] RULE: When auditing opening lines, cross-check against external databases (chessdb.cn, Lichess explorer) and the named source's actual recommendations, not just engine walks of the scripted moves | WHY: an engine walk validates our moves against the scripted opponent replies, but misses (a) stronger opponent replies that refute the line and (b) divergence from the repertoire source; the user's "many lines feel wrong" was confirmed this way after pure-engine audits had passed them.
- [2026-07-16] RULE: Never hand-build FEN strings; always generate them from move lists via chess.js | WHY: two hand-built FENs in one session had phantom pieces, producing garbage engine verdicts that had to be caught and re-run.
- [2026-08-01] RULE: Always re-fetch the chess.com archive immediately before any trend analysis; never reuse a snapshot from an earlier session | WHY: a snapshot fetched two days earlier was missing 8 games and happened to end mid-dip, which made a still-climbing rating look like a hard plateau. It produced a confidently wrong conclusion that was only caught by re-fetching.
- [2026-08-01] RULE: Never extrapolate a rating rate forward, and never say "keep doing what you're doing" from an aggregate monthly gain | WHY: ELO is exponential. +160 in July looked like "1000 in a month" but was one 26-game burst; the rate since was ~+0.4/game vs ~+6/game during the burst, a 15x slowdown. Check the recent slope and where the peak actually sits, not the month total.
- [2026-08-01] RULE: Actually run a shipped command end-to-end before declaring it done — unit tests passing is not the same as the command working | WHY: `chesscom-diagnostic.js` had a green test suite but running it for real revealed `--months 1` returns a near-empty report early in a calendar month (it counts archive months). A user following the docs would have gotten a one-game report.
- [2026-08-01] RULE: Check the actual measured data before prescribing standard chess-improvement advice | WHY: three separate confident claims were wrong for this user — "openings don't matter" (coverage is excellent and the Jaenisch line scores 89%), "you're rushing" (blunder moves take 2x LONGER than clean ones), and "you're not in time trouble" (30% of games end under 2 minutes; a median hid it). Each was only corrected by measuring.
- [2026-08-01] RULE: When a feature's own output proves it isn't serving the goal, delete it rather than fixing it | WHY: the puzzle generator ranked by eval-swing severity, so all 15 slots filled with rare forced-mate positions while 195 instances of the most common error never surfaced. The deeper problem was unfixable: skill needs thousands of pattern reps and ~15-50 positions/month can't compete with Lichess. Shipping the diagnostic half and deleting the rest was the right call.
- [2026-08-01] RULE: Prefer the sanctioned tool over handing the user a command to run | WHY: I told the user to merge the worktree branch themselves because `git push . HEAD:main` was refused and I'd been told not to `cd` into the main repo. ExitWorktree existed the whole time. Check for a proper tool before offloading work.

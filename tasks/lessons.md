- [2026-07-16] RULE: When auditing opening lines, cross-check against external databases (chessdb.cn, Lichess explorer) and the named source's actual recommendations, not just engine walks of the scripted moves | WHY: an engine walk validates our moves against the scripted opponent replies, but misses (a) stronger opponent replies that refute the line and (b) divergence from the repertoire source; the user's "many lines feel wrong" was confirmed this way after pure-engine audits had passed them.
- [2026-07-16] RULE: Never hand-build FEN strings; always generate them from move lists via chess.js | WHY: two hand-built FENs in one session had phantom pieces, producing garbage engine verdicts that had to be caught and re-run.
- [2026-08-01] RULE: Always re-fetch the chess.com archive immediately before any trend analysis; never reuse a snapshot from an earlier session | WHY: a snapshot fetched two days earlier was missing 8 games and happened to end mid-dip, which made a still-climbing rating look like a hard plateau. It produced a confidently wrong conclusion that was only caught by re-fetching.
- [2026-08-01] RULE: Never extrapolate a rating rate forward, and never say "keep doing what you're doing" from an aggregate monthly gain | WHY: ELO is exponential. +160 in July looked like "1000 in a month" but was one 26-game burst; the rate since was ~+0.4/game vs ~+6/game during the burst, a 15x slowdown. Check the recent slope and where the peak actually sits, not the month total.
- [2026-08-01] RULE: Actually run a shipped command end-to-end before declaring it done — unit tests passing is not the same as the command working | WHY: `chesscom-diagnostic.js` had a green test suite but running it for real revealed `--months 1` returns a near-empty report early in a calendar month (it counts archive months). A user following the docs would have gotten a one-game report.
- [2026-08-01] RULE: Check the actual measured data before prescribing standard chess-improvement advice | WHY: three separate confident claims were wrong for this user — "openings don't matter" (coverage is excellent and the Jaenisch line scores 89%), "you're rushing" (blunder moves take 2x LONGER than clean ones), and "you're not in time trouble" (30% of games end under 2 minutes; a median hid it). Each was only corrected by measuring.
- [2026-08-01] RULE: When a feature's own output proves it isn't serving the goal, delete it rather than fixing it | WHY: the puzzle generator ranked by eval-swing severity, so all 15 slots filled with rare forced-mate positions while 195 instances of the most common error never surfaced. The deeper problem was unfixable: skill needs thousands of pattern reps and ~15-50 positions/month can't compete with Lichess. Shipping the diagnostic half and deleting the rest was the right call.
- [2026-08-01] RULE: Prefer the sanctioned tool over handing the user a command to run | WHY: I told the user to merge the worktree branch themselves because `git push . HEAD:main` was refused and I'd been told not to `cd` into the main repo. ExitWorktree existed the whole time. Check for a proper tool before offloading work.
- [2026-08-01] RULE: An eval-swing classifier (before/after Stockfish delta) can flag a ply as a mistake even when the move played WAS the engine's own top choice — always check `playedMove !== engineBestMove` before presenting something as "the correct move was X." | WHY: on the real 109-game scan, 10/483 flagged instances (~2%) had `correctMoveSan` identical to the move actually played — mostly wild, already-decided endgames where a fixed-depth search sees further into a bad continuation than the "before" eval did. One of these ("you played Ke4, correct was Ke4") made it into the initial 15 selected puzzles before the check was added. The eval-swing count is still valid for aggregate diagnostic stats; it's specifically unsafe to treat as "here is what you should have played" without this check.
- [2026-08-02] RULE: When reusing an existing engine/component for a new content type, re-check every parameter it was tuned for against the new type's actual value range — don't assume "it already works for X" transfers to Y. | WHY: the drill engine's `playBaseMoves()` animates setup moves at 150ms each, fine for opening-line `baseMoves` (4-5). Tactics puzzles reused the same field name for a different meaning (the real ply number a mistake happened at, up to 105), so the identical code path took 15+ seconds on deep puzzles. Static checks (move legality, syntax, DOM-id existence) all passed — none of them exercise runtime timing/duration, so the bug only surfaced when the user actually used the feature. Confirmed via the user's "it takes too long" correction, not caught beforehand.
- [2026-08-02] RULE: Never use the Artifact tool unless explicitly asked — default to a local HTML file in the project (this is now a global rule in `~/.claude/CLAUDE.md`, not just project-specific). | WHY: user wants durable reference pages (training plans, dashboards, etc.) as real files they own in the repo, not hosted-elsewhere artifacts, even when the content would otherwise be a natural fit for one.
- [2026-08-02] RULE: Don't hand this user a recurring checklist of commands/tables to run themselves — trigger the whole routine off a phrase to me instead ("look at my latest games" = run the monthly diagnostic + puzzle refresh myself). | WHY: user explicitly rejected the Monthly section of `docs/training-ledger.html` — "I am not going to do this... have it in your memory for what all you need to do on your end." Saved as a standing memory ([[chess-monthly-review-trigger]] in the memory system) so this isn't relitigated.
- [2026-08-02] RULE: Chess training advice for this user must be a concrete drill (a named Lichess theme + link) or an in-game trigger tied to something already visible during play (the clock reading) — never a mental habit to remember mid-game (move counting, "remember to check"). | WHY: user rejected "watch the clock at move 30" (won't track move number mid-game) and "count the exchange before every capture" as a bare instruction (not trainable). Fixed by reframing around the clock number itself and by naming the exact Lichess puzzle themes (`hangingPiece`, `capturingDefender`) that train the same skill. Also saved as a memory for future training-content work in this project.
